fn fib(n: i64) -> i64 {
	if(0 == n){
		0;
	}
	else {
		if(1 == n){
			1;
		}
		else{
			this(n - 1) + this(n - 2);
		}
	}
}

fn main() -> i64 {
	let a: i64 = 6;

	println(a);
	println("fib(", 5, ") = ", fib(5));
	println("fib(", 10, ") = ", fib(10));
	println("fib(", 15, ") = ", fib(15));
	println("fib(", 20, ") = ", fib(20));
}
